{
  ;() => {
    const PRICE_PER_UNIT: number = 1000.21312
    let stock: number = 20
    const price: number = stock * PRICE_PER_UNIT
    const MINIMUN_STOCK = 30
    console.log(PRICE_PER_UNIT.toFixed(2))
    stock = Number('55A')
  }
}
